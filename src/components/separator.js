'use client'


export default function Separator({ color, width, height }) {
    return (
        <div style={{ backgroundColor: color, width: width, height: height, margin: "10px 0" }}>
        </div>
    )
}
